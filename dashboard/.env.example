# --- API du bot (Tailscale Funnel) ---
API_BASE_URL=https://barde.tail2985e8.ts.net
API_TOKEN=le_meme_token_que_dans_le_.env_du_bot

# --- Discord OAuth (app d'un bot existant) ---
DISCORD_CLIENT_ID=xxxxxxxxxxxxxxxxx
DISCORD_CLIENT_SECRET=xxxxxxxxxxxxxxxxx

# IDs Discord autorisés à se connecter, séparés par des virgules (toi + le fonda)
ALLOWED_DISCORD_IDS=123456789012345678,987654321098765432

# --- Auth.js ---
# Généré avec: openssl rand -base64 32   (ou npx auth secret)
AUTH_SECRET=une_chaine_aleatoire_longue
# En prod Vercel, laisser vide (auto-détecté). En local: http://localhost:3000
# AUTH_URL=http://localhost:3000

# --- Auth du JEU (MYRHAVEN) — publique, pas de whitelist, séparée de l'admin ---
# Même app OAuth Discord que ci-dessus (scope identify), il faut juste ajouter
# <ce domaine>/api/game/auth/callback dans OAuth2 > Redirects sur le portail Discord.
# Signe la session joueur et le state OAuth. Généré avec: openssl rand -base64 32
GAME_SESSION_SECRET=une_autre_chaine_aleatoire_longue
# Origine publique du jeu (autorisée en CORS + seule cible de redirection acceptée)
GAME_ORIGIN=https://myrhaven.example.com

# --- Édition de hotspots en direct (jeu MYRHAVEN, éditeur F2, touche S hors dev) ---
# Écrit les fichiers de hotspots du jeu via l'API GitHub — token fin (fine-grained PAT),
# limité au seul repo EpSyDev/myrhaven-point-and-click, permission "Contents: Read and write".
GAME_RECTS_GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# IDs Discord autorisés à écrire des hotspots depuis le jeu, séparés par des virgules — même
# personnes qu'ALLOWED_DISCORD_IDS aujourd'hui, mais liste dédiée (voir lib/gameRects.ts)
GAME_RECTS_ADMIN_IDS=123456789012345678,987654321098765432
